# AM++

AM++ 为 Apple Music 提供可独立启用的播放与歌词体验增强，同时保持目标应用原有播放流程可用。

## Language

**双向歌词模糊**:
以当前高亮歌词为清晰中心，历史歌词与后续歌词都随距离增加而逐渐模糊的视觉处理。
_Avoid_: 未来歌词模糊、单向歌词模糊

**滚动暂停**:
用户手动浏览歌词时暂时移除双向歌词模糊，并在滚动稳定后恢复当前高亮位置对应的视觉状态。
_Avoid_: 永久清除模糊、等待下一句恢复

**目标符号**:
Apple Music 内部被 AM++ 功能定位并使用的类或方法；每个目标符号都必须能独立报告找到、缺失或歧义。
_Avoid_: 任意反射对象、静默首个匹配

**版本 profile**:
针对一个经过验证的 Apple Music 精确版本，集中记录其目标符号身份与契约的适配知识。
_Avoid_: 对所有版本通用的猜测、散落的混淆名

**配置 schema**:
设置进程与 Apple Music 目标进程共享的配置键、默认值、编码和升级规则；任何进程都不得自行复制这些规则。
_Avoid_: 配置常量集合、各进程独立读取
