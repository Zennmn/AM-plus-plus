package com.apple.android.music.icloud.api

/** Mirrors the interface-based iCloud Accept-Language helper AMTool hooks with xi(1). */
interface ICloudAcceptLanguageFixture {
    fun acceptLanguage(): String
}
