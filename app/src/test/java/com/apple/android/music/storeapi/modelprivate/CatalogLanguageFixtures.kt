package com.apple.android.music.storeapi.modelprivate

/** Same binary name shape as AMTool's Store lookup seam; setParam returns the builder. */
class Request {
    class Builder {
        @Suppress("UNUSED_PARAMETER")
        fun setParam(key: String, value: String): Builder = this
    }
}
