package com.apple.android.music.mediaapi

/** Mirrors the l/lang/locale language-param map seam AMTool hooks with xi(2). */
class LanguageParamsFixture {
    fun languageParams(): Map<String, Any> = emptyMap()
}
